
-  x = "Dev" if Sinatra::Base.environment == :development
-  x = "Test" if Sinatra::Base.environment == :test
-  x = "Prod" if Sinatra::Base.environment == :production
h2 We are in the #{x} environment.
